import React from "react";
import Hero from "@/components/hero";
import Navbar from "@/components/navbar";

const page = () => {
  return (
    <div>
      <Navbar/>
      <Hero />
    </div>
  );
};

export default page;
